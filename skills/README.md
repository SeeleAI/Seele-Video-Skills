# Skills

Each public skill should live in its own directory and include:

- `SKILL.md` with purpose, triggers, inputs, outputs, and operating constraints
- dependency and model requirements
- a minimal reproducible example
- license and asset provenance notes
- a security review confirming that no credentials or private endpoints are included
